#include "hammer.h"
#include "failure.h"
#include "smckl.h"

WIDTH
HEIGHT

int xtickmarkspacing = 2400/WIDTH+1; 
           for (int j=BORDER_OFFSET; j<n; j=j+xtickmarkspacing) { 
                g.drawLine(Px[j], HEIGHT/2+10, Px[j], HEIGHT/2-10); 
                String str = new Double(roundDown(points.get(j).x,1)).toString();
              g.drawString(str, Px[j]-20, HEIGHT/2+20);
            }
